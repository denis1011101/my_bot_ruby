#!/usr/bin/env ruby


require 'listen'
require 'listen/cli'

if !defined?(JRUBY_VERSION) && Signal.list.keys.include?('INT')
  Signal.trap('INT') { Thread.new { Listen.stop } }
end

Listen::CLI.start
